# Learn c++ as a second language

## Printing

```cpp
cout << "Hello World";
```

### new lines

New lines need to be added separately

```cpp
cout << "Hello World\n";
```

## Something long

```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
	cin.tie(0)->sync_with_stdio(0);
	cin >> s;
  cout << s;
}
```

## Some table

| name  | description  |
| ----- | ------------ |
| btree | code academy |

> Quote test **Publish** sub-menu.

## KaTeX

inline: $\Gamma(n) = (n-1)!\quad\forall n\in\mathbb N$

multiline:

$$
\Gamma(z) = \int_0^\infty t^{z-1}e^{-t}dt\,.
$$

